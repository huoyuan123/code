核心区别
大小确定时机
静态数组：大小在编译期确定，如 int a[100];
动态数组：大小在运行期决定，用 malloc/calloc/realloc 分配，如 int* p = malloc(n*sizeof *p);
注：C99 有 VLA（变长数组）int a[n]; 在栈上分配，属于“运行期决定大小的栈数组”，不是堆数组。MSVC 不支持 VLA；MinGW/GCC 一般支持。

存储区域与生命周期
静态数组：
全局/static 声明 → 静态存储区，程序整个生命周期存在，默认为零初始化。
函数内非 static → 栈（stack），作用域结束自动释放，默认不初始化（是“垃圾值”）。
动态数组：
堆（heap），显式 malloc/calloc 获取，free 释放；不释放会内存泄漏。
calloc 会零初始化；malloc 不会。

类型与 sizeof 行为
静态数组变量是“数组类型”。在表达式中常退化为指向首元素的指针，但 sizeof(数组变量) 返回整个数组字节数。
例：int a[100]; sizeof(a) == 100*sizeof(int)。
动态数组用“指针变量”指向堆内存。sizeof(指针) 只返回指针大小（如 8 字节），长度需自行记录。
例：int* p = malloc(n*sizeof *p); sizeof(p) == 指针大小，与 n 无关。

可扩展性与重分配
静态数组：大小固定，不能扩展。
动态数组：可用 realloc 调整大小（成功后地址可能变化）。

赋值/拷贝/参数传递
数组不能整体赋值（只能初始化时用花括号）；要复制内容需 memcpy/循环。
函数参数的数组形参会退化为指针（本质上传的是指针）。
函数不能“返回数组类型”，但可以返回指向堆内存的指针（注意所有权管理）。返回局部栈数组的地址是错误的。

性能与风险
栈分配（静态/自动存储期）非常快，但容量有限（可能栈溢出）。
堆分配有分配/释放开销，可能碎片化；需要正确 free，避免泄漏/重复释放/悬空指针。

总结
静态数组适合大小已知且不变的场景，简单高效。
动态数组适合大小动态变化的需求，但需手动管理内存。